sudo apt-get install memcached
memcached -d -m 64 -u root -l 127.0.0.1 -p 11211 -c 1024 -P /tmp/memcached.log
ftp://ftp.tummy.com/pub/python-memcached/python-memcached-1.48.tar.gz
sudo python setup.py install

tar czf nng.tar.gz nng

# start apache2 gzip
sudo a2enmod deflate


设置 media 文件夹为所有用户和读写

sudo a2ensite nng.com
sudo service apache2 reload

sudo pip install pytz
